[project]
name = "gauntlet"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["ollama"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
